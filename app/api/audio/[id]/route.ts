import { env } from "cloudflare:workers";
import { apiError, requireUser } from "../../../../lib/auth";
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){try{const user=await requireUser(request),{id}=await params,asset=await env.DB.prepare("SELECT storage_key storageKey,mime_type mimeType,file_size fileSize FROM audio_assets WHERE id=? AND owner_id=?").bind(id,user.id).first<{storageKey:string;mimeType:string;fileSize:number}>();if(!asset)return new Response("Not found",{status:404});const range=request.headers.get("range");if(range){const match=/^bytes=(\d*)-(\d*)$/.exec(range);if(!match)return new Response("Invalid range",{status:416});const start=match[1]?Number(match[1]):0,end=match[2]?Math.min(Number(match[2]),asset.fileSize-1):asset.fileSize-1;if(start>end||start>=asset.fileSize)return new Response(null,{status:416,headers:{"content-range":`bytes */${asset.fileSize}`}});const object=await env.AUDIO.get(asset.storageKey,{range:{offset:start,length:end-start+1}});if(!object)return new Response("Not found",{status:404});return new Response(object.body,{status:206,headers:{"content-type":asset.mimeType,"accept-ranges":"bytes","content-range":`bytes ${start}-${end}/${asset.fileSize}`,"content-length":String(end-start+1),"cache-control":"private, max-age=3600"}})}const object=await env.AUDIO.get(asset.storageKey);if(!object)return new Response("Not found",{status:404});return new Response(object.body,{headers:{"content-type":asset.mimeType,"content-length":String(asset.fileSize),"accept-ranges":"bytes","cache-control":"private, max-age=3600"}})}catch(error){return apiError(error)}}
