import { env } from "cloudflare:workers";
import { getSql } from "../../../db";
import { apiError, requireUser } from "../../../lib/auth";
import { compose, validateGeneration } from "../../../lib/domain";
import { CloudflareGenerationQueue, InlineTestGenerationQueue } from "../../../lib/generation-queue";
import { GenerationOrchestrator } from "../../../lib/generation-orchestrator";
import { R2AudioStorage } from "../../../lib/audio-storage";
import { createProvider } from "../../../lib/providers";
import { validateElevenLabsPolicy } from "../../../lib/provider-policy";
import { HttpVocalIdentityProcessor } from "../../../lib/vocal-identity-processor";
import type { AppBindings } from "../../../lib/config";
const bindings=env as unknown as AppBindings;
function sql(){return getSql(bindings.DATABASE_URL)}
function dto(r:Record<string,unknown>){return{id:r.id,songId:r.songId,title:r.title,prompt:r.prompt,createdAt:r.createdAt,version:r.version||1,duration:r.duration||r.planDuration,bpm:r.bpm,musicalKey:`${r.musicalKey} ${r.scale}`,genre:r.genre,provider:r.provider,vocalist:r.vocalist||undefined,status:r.status,errorMessage:r.errorMessage,progress:r.progress,seed:r.seed,waveform:r.waveform||[],audioUrl:r.audioAssetId?`/api/audio/${r.audioAssetId}`:undefined}}
const select=`select j.id,s.id as "songId",s.title,j.request_payload->>'prompt' prompt,j.created_at as "createdAt",v.version_number version,v.duration_seconds duration,(j.composition_plan->>'durationSeconds')::float8 as "planDuration",(j.composition_plan->>'bpm')::int bpm,j.composition_plan->>'key' as "musicalKey",j.composition_plan->>'scale' scale,j.composition_plan->>'genre' genre,j.provider,case when vp.id is not null then vp.name||' · My Voice V'||vpv.version_number::text end as vocalist,j.status,case when j.error_code='PROVIDER_PROMPT_REJECTED' then 'Prompt references restricted copyrighted or artist-identifying material.' when j.error_code='GENERATION_PROVIDER_UNAVAILABLE' then 'The music service is temporarily unavailable. Wait a moment and safely try again.' when j.error_code='PROVIDER_AUTHORIZATION_FAILED' then 'The music-service key was not accepted. Update the provider key and try again.' when j.error_code='PROVIDER_RATE_LIMITED' then 'The music service is busy. Wait a moment and safely try again.' when j.error_code='GENERATION_TIMEOUT' then 'Generation took too long. You can safely try again.' when j.error_code='STORAGE_UNAVAILABLE' then 'Dozi could not save the finished audio. Restart Dozi and try again.' when j.error_code='VOCAL_IDENTITY_PROCESSOR_UNAVAILABLE' then 'The private vocalist service is not running.' when j.error_code='VOCAL_IDENTITY_SEPARATION_FAILED' then 'Dozi could not isolate a usable lead vocal.' when j.error_code='VOCAL_IDENTITY_CONVERSION_FAILED' then 'Dozi could not apply the selected private vocalist.' when j.error_code='VOCAL_PROFILE_MODEL_UNAVAILABLE' then 'The selected private vocalist model is unavailable.' when j.status='FAILED' then 'Generation failed.' end as "errorMessage",j.progress,j.seed,a.waveform_data waveform,a.id as "audioAssetId" from generation_jobs j join songs s on s.id=j.song_id left join song_versions v on v.generation_job_id=j.id left join audio_assets a on a.id=v.audio_asset_id left join artist_vocal_profiles vp on vp.id=j.vocal_profile_id left join vocal_profile_versions vpv on vpv.id=j.vocal_profile_version_id`;
export async function GET(request:Request){try{const user=await requireUser(request,bindings.DATABASE_URL),database=sql(),rows=await database<Record<string,unknown>[]>`${database.unsafe(select)} where j.user_id=${user.id} and s.archived_at is null order by j.created_at desc limit 100`;return Response.json({songs:rows.map(dto)})}catch(error){return apiError(error)}}
export async function POST(request:Request){try{const user=await requireUser(request,bindings.DATABASE_URL),input=validateGeneration(await request.json()),idempotencyKey=request.headers.get("idempotency-key")||crypto.randomUUID(),providerName=bindings.MUSIC_PROVIDER||"mock";if(providerName==="elevenlabs")validateElevenLabsPolicy(input);if(input.instrumental&&input.vocalProfileId)throw new Error("INVALID_VOCAL_PROFILE_SELECTION");const providerConfig={aiServiceBaseUrl:bindings.AI_SERVICE_BASE_URL,aiServiceToken:bindings.AI_SERVICE_TOKEN,aceStepModel:bindings.ACESTEP_MODEL,minimaxModel:bindings.MINIMAX_MODEL,elevenLabsApiKey:bindings.ELEVENLABS_API_KEY,elevenLabsModel:bindings.ELEVENLABS_MODEL},provider=createProvider(providerName,providerConfig),database=sql();const existing=await database<Record<string,unknown>[]>`${database.unsafe(select)} where j.user_id=${user.id} and j.idempotency_key=${idempotencyKey} limit 1`;if(existing[0])return Response.json({song:dto(existing[0])});let vocalSelection:{profileId:string;profileVersionId:string;name:string;versionNumber:number}|null=null;if(input.vocalProfileId){const selected=await database<{profileId:string;profileVersionId:string;name:string;versionNumber:number}[]>`select p.id as "profileId",v.id as "profileVersionId",p.name,v.version_number as "versionNumber" from artist_vocal_profiles p join vocal_profile_versions v on v.profile_id=p.id and v.is_active=true and v.status='READY' where p.id=${input.vocalProfileId} and p.owner_id=${user.id} and p.status='ACTIVE' and p.revoked_at is null and v.revoked_at is null limit 1`;vocalSelection=selected[0]||null;if(!vocalSelection)throw new Error("VOCAL_PROFILE_UNAVAILABLE")}const plan=compose(input),songId=crypto.randomUUID(),jobId=crypto.randomUUID(),versionId=crypto.randomUUID(),seed=input.seed??Math.floor(Math.random()*2147483647),title=plan.titleSuggestions[0]||"Untitled song";await database.begin(async tx=>{await tx`insert into songs(id,user_id,title,description,lyrics,is_instrumental,next_version_number) values(${songId},${user.id},${title},${input.prompt},${input.lyrics||""},${input.instrumental},2)`;await tx`insert into generation_jobs(id,version_id,user_id,song_id,vocal_profile_id,vocal_profile_version_id,reserved_version_number,idempotency_key,provider,provider_model,status,progress,request_payload,composition_plan,seed) values(${jobId},${versionId},${user.id},${songId},${vocalSelection?.profileId||null},${vocalSelection?.profileVersionId||null},1,${idempotencyKey},${provider.name},${provider.model},'QUEUED',4,${tx.json(input)},${tx.json(plan)},${seed})`});const vocalIdentityProcessor=bindings.AI_SERVICE_BASE_URL?new HttpVocalIdentityProcessor(bindings.AI_SERVICE_BASE_URL,bindings.AI_SERVICE_TOKEN):undefined,orchestrator=new GenerationOrchestrator(database,new R2AudioStorage(bindings.AUDIO),name=>createProvider(name,providerConfig),vocalIdentityProcessor),queue=bindings.GENERATION_QUEUE?new CloudflareGenerationQueue(bindings.GENERATION_QUEUE):new InlineTestGenerationQueue(id=>orchestrator.process(id));await queue.enqueue(jobId);return Response.json({song:{id:jobId,songId,title,prompt:input.prompt,createdAt:new Date().toISOString(),version:1,duration:plan.durationSeconds,bpm:plan.bpm,musicalKey:`${plan.key} ${plan.scale}`,genre:plan.genre,provider:provider.name,vocalist:vocalSelection?`${vocalSelection.name} · My Voice V${vocalSelection.versionNumber}`:undefined,status:"QUEUED",progress:4,seed,waveform:[]}},{status:202})}catch(error){return apiError(error)}}
