import { env } from "cloudflare:workers";
import { getSql } from "../../../../db";
import { apiError, requireUser } from "../../../../lib/auth";
import type { AppBindings } from "../../../../lib/config";
const bindings=env as unknown as AppBindings;
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){try{const user=await requireUser(request,bindings.DATABASE_URL),{id}=await params,sql=getSql(bindings.DATABASE_URL),rows=await sql<Record<string,unknown>[]>`select j.id,s.id as "songId",s.title,j.request_payload->>'prompt' prompt,j.created_at as "createdAt",coalesce(v.version_number,1) version,coalesce(v.duration_seconds,(j.composition_plan->>'durationSeconds')::float8) duration,(j.composition_plan->>'bpm')::int bpm,j.composition_plan->>'key' as key,j.composition_plan->>'scale' scale,j.composition_plan->>'genre' genre,j.provider,case when vp.id is not null then vp.name||' · My Voice V'||vpv.version_number::text end as vocalist,j.status,case when j.error_code='PROVIDER_PROMPT_REJECTED' then 'Prompt references restricted copyrighted or artist-identifying material.' when j.error_code='GENERATION_PROVIDER_UNAVAILABLE' then 'The music service is temporarily unavailable. Wait a moment and safely try again.' when j.error_code='PROVIDER_AUTHORIZATION_FAILED' then 'The music-service key was not accepted. Update the provider key and try again.' when j.error_code='PROVIDER_RATE_LIMITED' then 'The music service is busy. Wait a moment and safely try again.' when j.error_code='GENERATION_TIMEOUT' then 'Generation took too long. You can safely try again.' when j.error_code='STORAGE_UNAVAILABLE' then 'Dozi could not save the finished audio. Restart Dozi and try again.' when j.error_code='VOCAL_IDENTITY_PROCESSOR_UNAVAILABLE' then 'The private vocalist service is not running.' when j.error_code='VOCAL_IDENTITY_SEPARATION_FAILED' then 'Dozi could not isolate a usable lead vocal.' when j.error_code='VOCAL_IDENTITY_CONVERSION_FAILED' then 'Dozi could not apply the selected private vocalist.' when j.error_code='VOCAL_PROFILE_MODEL_UNAVAILABLE' then 'The selected private vocalist model is unavailable.' when j.status='FAILED' then 'Generation failed.' end as "errorMessage",j.progress,j.seed,coalesce(a.waveform_data,'[]'::jsonb) waveform,a.id as "audioAssetId" from generation_jobs j join songs s on s.id=j.song_id left join song_versions v on v.generation_job_id=j.id left join audio_assets a on a.id=v.audio_asset_id left join artist_vocal_profiles vp on vp.id=j.vocal_profile_id left join vocal_profile_versions vpv on vpv.id=j.vocal_profile_version_id where j.id=${id} and j.user_id=${user.id} limit 1`;if(!rows[0])return Response.json({error:{code:"NOT_FOUND",message:"Generation not found.",retryable:false}},{status:404});const r=rows[0];return Response.json({song:{...r,musicalKey:`${r.key} ${r.scale}`,audioUrl:r.audioAssetId?`/api/audio/${r.audioAssetId}`:undefined}})}catch(error){return apiError(error)}}
export async function DELETE(request:Request,{params}:{params:Promise<{id:string}>}){try{const user=await requireUser(request,bindings.DATABASE_URL),{id}=await params,sql=getSql(bindings.DATABASE_URL),rows=await sql`update generation_jobs set status=case when status='QUEUED' then 'CANCELLED' else status end,cancellation_requested_at=now(),updated_at=now() where id=${id} and user_id=${user.id} and status not in ('COMPLETE','FAILED','CANCELLED') returning id`;return Response.json({cancelled:Boolean(rows[0])})}catch(error){return apiError(error)}}
