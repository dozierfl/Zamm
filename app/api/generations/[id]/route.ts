import { env } from "cloudflare:workers";
import { getSql } from "../../../../db";
import { apiError, requireUser } from "../../../../lib/auth";
import type { AppBindings } from "../../../../lib/config";
const bindings=env as unknown as AppBindings;
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){try{const user=await requireUser(request,bindings.DATABASE_URL),{id}=await params,sql=getSql(bindings.DATABASE_URL),rows=await sql<Record<string,unknown>[]>`select j.id,s.title,j.request_payload->>'prompt' prompt,j.created_at as "createdAt",coalesce(v.version_number,1) version,coalesce(v.duration_seconds,(j.composition_plan->>'durationSeconds')::int) duration,(j.composition_plan->>'bpm')::int bpm,j.composition_plan->>'key' as key,j.composition_plan->>'scale' scale,j.composition_plan->>'genre' genre,j.provider,j.status,case when j.error_code='PROVIDER_PROMPT_REJECTED' then 'Prompt references restricted copyrighted or artist-identifying material.' when j.status='FAILED' then 'Generation failed.' end as "errorMessage",j.progress,j.seed,coalesce(a.waveform_data,'[]'::jsonb) waveform,a.id as "audioAssetId" from generation_jobs j join songs s on s.id=j.song_id left join song_versions v on v.generation_job_id=j.id left join audio_assets a on a.id=v.audio_asset_id where j.id=${id} and j.user_id=${user.id} limit 1`;if(!rows[0])return Response.json({error:{code:"NOT_FOUND",message:"Generation not found.",retryable:false}},{status:404});const r=rows[0];return Response.json({song:{...r,musicalKey:`${r.key} ${r.scale}`,audioUrl:r.audioAssetId?`/api/audio/${r.audioAssetId}`:undefined}})}catch(error){return apiError(error)}}
export async function DELETE(request:Request,{params}:{params:Promise<{id:string}>}){try{const user=await requireUser(request,bindings.DATABASE_URL),{id}=await params,sql=getSql(bindings.DATABASE_URL),rows=await sql`update generation_jobs set status=case when status='QUEUED' then 'CANCELLED' else status end,cancellation_requested_at=now(),updated_at=now() where id=${id} and user_id=${user.id} and status not in ('COMPLETE','FAILED','CANCELLED') returning id`;return Response.json({cancelled:Boolean(rows[0])})}catch(error){return apiError(error)}}
