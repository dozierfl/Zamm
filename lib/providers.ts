import type { CompositionPlan } from "./domain";
import { createDeterministicWav } from "./wav";
export type ProviderCapabilities={textToMusic:boolean;lyrics:boolean;referenceAudio:boolean;continuation:boolean;repaint:boolean;stems:boolean;bpmControl:boolean;keyControl:boolean;seed:boolean};
export interface MusicGenerationProvider{name:string;model:string;capabilities():ProviderCapabilities;healthCheck():Promise<{available:boolean;latencyMs:number;message:string}>;generate(request:{plan:CompositionPlan;seed:number}):ReturnType<typeof createDeterministicWav>}
export class MockMusicProvider implements MusicGenerationProvider{name="mock";model="dozi-deterministic-wav-v1";capabilities(){return{textToMusic:true,lyrics:true,referenceAudio:false,continuation:false,repaint:false,stems:false,bpmControl:true,keyControl:true,seed:true}}async healthCheck(){return{available:true,latencyMs:0,message:"Deterministic local provider ready"}}generate({plan,seed}:{plan:CompositionPlan;seed:number}){return createDeterministicWav(seed,plan.durationSeconds,plan.bpm)}}
export class AceStepMusicProvider implements MusicGenerationProvider{readonly name="acestep";readonly model="unconfigured";capabilities():ProviderCapabilities{return{textToMusic:false,lyrics:false,referenceAudio:false,continuation:false,repaint:false,stems:false,bpmControl:false,keyControl:false,seed:false}}async healthCheck(){return{available:false,latencyMs:0,message:"ACE-Step requires a documented service URL and model configuration"}}async generate():Promise<never>{throw new Error("GENERATION_PROVIDER_UNAVAILABLE")}}
export const providers={mock:new MockMusicProvider(),acestep:new AceStepMusicProvider()};
