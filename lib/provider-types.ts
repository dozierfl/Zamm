import type { ContextualTrackGenerationRequest, GenerationRequest, GenerationResult, ProviderCapabilities, ProviderHealth } from "./domain";
export interface MusicGenerationProvider{name:string;model:string;capabilities():ProviderCapabilities;healthCheck():Promise<ProviderHealth>;generate(request:GenerationRequest):Promise<GenerationResult>;generateContextualTrack?(request:ContextualTrackGenerationRequest):Promise<GenerationResult>;cancel?(jobId:string):Promise<void>}
